spring:
  datasource:
    url: jdbc:mysql://localhost:3306/smart_camera?useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    com.smartcamera: DEBUG
    io.netty: DEBUG
